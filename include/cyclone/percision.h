namespace cyclone {
   /*
   Defines a real number percision. Cyclone can be compiled in 
   single(32bit)- or double(64bit)-percision versions. By default, single 
   precision is provided.
   */
   typedef float real;

   /*
   defines the recision of the power oprator
   */
   #define real_pow powf
}